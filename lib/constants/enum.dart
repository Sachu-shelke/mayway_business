enum EnvironmentType { development, production }

enum UserType { pending, register, login }

enum Range { lowToHigh, highToLow, aToz, zToa }

enum GiftFor { mySelf, friend }

enum MobileRechargeType { prepaid, postpaid }

enum RechargePaymentDiscount { cashbackPoints, notCashback }

enum AppStoreTime { youtube, telegram, instagram, notStore }

enum RechargeType {
  postpaid,
  prepaid,
  dthRecharge,
  electricityRecharge,
  fastag,
  landLine,
  lPGGas,
  naturalGas,
  brodBand,
  water,
  hospital,
  municipality,
  housingPayment,
  feePayment,
}
